name: CI

on:
  push:
    branches: ['**']
    tags: ['v*']
  pull_request:
  workflow_dispatch:

concurrency:
  group: curier-${{ github.ref }}
  cancel-in-progress: false

env:
  PIOARDUINO_PLATFORM_URL: https://github.com/pioarduino/platform-espressif32/releases/download/55.03.39/platform-espressif32.zip
  PIOARDUINO_PLATFORM_VERSION: 55.03.39
  PIOARDUINO_VERSION: 6.1.19
  STRATA_VERSION: v0.1.2

jobs:
  metadata:
    runs-on: [self-hosted, zekstack-ci]
    steps:
      - uses: actions/checkout@v4
      - name: Validate release metadata
        run: python3 scripts/validate_release_version.py

  clang-format:
    runs-on: [self-hosted, zekstack-ci]
    steps:
      - uses: actions/checkout@v4
      - name: Install clang-format
        run: sudo apt-get update && sudo apt-get install -y clang-format
      - name: Check formatting
        run: |
          find src tests examples extras -type f \
            \( -name '*.h' -o -name '*.cpp' -o -name '*.ino' \) -print0 \
            | xargs -0 clang-format --dry-run --Werror

  source-audit:
    runs-on: [self-hosted, zekstack-ci]
    steps:
      - uses: actions/checkout@v4
      - name: Audit library boundaries, protocol, lifecycle, and ownership
        run: |
          set -e
          if grep -RInE '#include[[:space:]]+[<"](Accord|Flow|Fresh|Link|Phase|Pulse|Seal|Signal|Tempo|Trace|Worker)\.h[>"]' src; then
            echo "Curier must not depend on another ZekStack library except Strata"
            exit 1
          fi
          if grep -RInE '"aesgcm"|Content-Encoding:[[:space:]]+aesgcm' src; then
            echo "Legacy aesgcm encoding is forbidden"
            exit 1
          fi
          if grep -RIn '_impl\.release' src; then
            echo "Curier must not leak its implementation from callback destruction"
            exit 1
          fi
          if grep -RIn 'deleteCurrent' src; then
            echo "Curier workers must be deleted by the owner task"
            exit 1
          fi
          if grep -RInE '#include[[:space:]]+[<"]esp_heap_caps\.h[>"]|freertos/idf_additions\.h' src; then
            echo "Curier must not depend on ESP-IDF allocation internals"
            exit 1
          fi
          if grep -RInE 'heap_caps_|MALLOC_CAP_|ps_malloc|xTaskCreate|vTaskDelete|xQueueCreate|xSemaphoreCreate|std::make_unique|std::make_shared|(^|[^[:alnum:]_])malloc[[:space:]]*\(|(^|[^[:alnum:]_])calloc[[:space:]]*\(|(^|[^[:alnum:]_])realloc[[:space:]]*\(|(^|[^[:alnum:]_])free[[:space:]]*\(|(^|[^[:alnum:]_])new[[:space:](]|(^|[^[:alnum:]_])delete[[:space:](]' src; then
            echo "Curier-owned allocation and FreeRTOS storage must route through Strata"
            exit 1
          fi

  host-tests:
    runs-on: [self-hosted, zekstack-ci]
    needs: source-audit
    steps:
      - uses: actions/checkout@v4
      - name: Fetch Strata host backend
        run: |
          STRATA_DIR="$RUNNER_TEMP/strata"
          rm -rf "$STRATA_DIR"
          git clone --depth 1 --branch "${STRATA_VERSION}" \
            https://github.com/ZekStack/strata.git "$STRATA_DIR"
          echo "STRATA_DIR=$STRATA_DIR" >> "$GITHUB_ENV"
      - name: Build and run host protocol tests
        run: |
          g++ -std=c++20 -Wall -Wextra -pedantic \
            -I"$STRATA_DIR/src" \
            -Itests/host/stubs \
            -Isrc \
            "$STRATA_DIR/src/strata/Allocation.cpp" \
            "$STRATA_DIR/src/strata/internal/PlatformGeneric.cpp" \
            src/internal/CurierProtocol.cpp \
            src/internal/CurierRetry.cpp \
            tests/host/test_retry_protocol.cpp \
            -o /tmp/curier-host-tests
          /tmp/curier-host-tests

  crypto-tests:
    runs-on: [self-hosted, zekstack-ci]
    needs: source-audit
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Fetch Strata host backend
        run: |
          STRATA_DIR="$RUNNER_TEMP/strata"
          rm -rf "$STRATA_DIR"
          git clone --depth 1 --branch "${STRATA_VERSION}" \
            https://github.com/ZekStack/strata.git "$STRATA_DIR"
          echo "STRATA_DIR=$STRATA_DIR" >> "$GITHUB_ENV"
      - name: Install host crypto dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libmbedtls-dev
          python -m pip install --upgrade "pioarduino==${PIOARDUINO_VERSION}"
          pio pkg install -g -l "bblanchon/ArduinoJson@7.0.0"
      - name: Build and run RFC 8291 and VAPID tests
        run: |
          ARDUINOJSON_HEADER="$(find ~/.platformio -type f -path '*/ArduinoJson/src/ArduinoJson.h' -print -quit)"
          test -n "$ARDUINOJSON_HEADER"
          ARDUINOJSON_INCLUDE="$(dirname "$ARDUINOJSON_HEADER")"
          g++ -std=c++20 -Wall -Wextra -pedantic \
            -I"$STRATA_DIR/src" \
            -I"$ARDUINOJSON_INCLUDE" \
            -Itests/host/stubs \
            -Isrc \
            "$STRATA_DIR/src/strata/Allocation.cpp" \
            "$STRATA_DIR/src/strata/internal/PlatformGeneric.cpp" \
            src/internal/CurierCrypto.cpp \
            tests/host/test_crypto.cpp \
            -lmbedtls -lmbedx509 -lmbedcrypto \
            -o /tmp/curier-crypto-tests
          /tmp/curier-crypto-tests

  build-examples:
    runs-on: [self-hosted, zekstack-ci]
    needs: [source-audit, host-tests, crypto-tests]
    strategy:
      fail-fast: false
      matrix:
        board:
          - esp32dev
          - esp32-s3-devkitc-1
          - esp32-c3-devkitm-1
          - esp32-p4-evboard
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Cache PIOArduino
        uses: actions/cache@v4
        with:
          path: |
            ~/.platformio
            ~/.cache/pip
          key: ${{ runner.os }}-pioarduino-${{ env.PIOARDUINO_VERSION }}-${{ env.PIOARDUINO_PLATFORM_VERSION }}-${{ hashFiles('**/library.json') }}
      - name: Install PIOArduino
        run: |
          python -m pip install --upgrade "pioarduino==${PIOARDUINO_VERSION}"
          pio pkg install -g -p "${PIOARDUINO_PLATFORM_URL}"
      - name: Build examples
        run: |
          set -e
          for sketch in examples/*; do
            pio ci "$sketch" \
              --board "${{ matrix.board }}" \
              --lib="." \
              --project-option "platform=${PIOARDUINO_PLATFORM_URL}" \
              --project-option "lib_deps=bblanchon/ArduinoJson@7.0.0, https://github.com/ZekStack/strata.git#${STRATA_VERSION}" \
              --project-option "build_unflags=-std=gnu++11" \
              --project-option "build_flags=-std=gnu++20"
          done
      - name: Compile lifecycle stress test
        run: |
          pio ci tests/esp32/LifecycleSmoke \
            --board "${{ matrix.board }}" \
            --lib="." \
            --project-option "platform=${PIOARDUINO_PLATFORM_URL}" \
            --project-option "lib_deps=bblanchon/ArduinoJson@7.0.0, https://github.com/ZekStack/strata.git#${STRATA_VERSION}" \
            --project-option "build_unflags=-std=gnu++11" \
            --project-option "build_flags=-std=gnu++20"
      - name: Compile runtime semantics test
        if: matrix.board == 'esp32dev'
        run: |
          pio ci tests/esp32/RuntimeSemantics \
            --board "${{ matrix.board }}" \
            --lib="." \
            --project-option "platform=${PIOARDUINO_PLATFORM_URL}" \
            --project-option "lib_deps=bblanchon/ArduinoJson@7.0.0, https://github.com/ZekStack/strata.git#${STRATA_VERSION}" \
            --project-option "build_unflags=-std=gnu++11" \
            --project-option "build_flags=-std=gnu++20 -DCURIER_ENABLE_TEST_HOOKS"

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [metadata, clang-format, source-audit, host-tests, crypto-tests, build-examples]
    runs-on: [self-hosted, zekstack-ci]
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          awk -v version="$VERSION" '
            $0 == "## " version {capture=1; next}
            capture && /^## / {exit}
            capture {print}
          ' CHANGELOG.md > /tmp/release-notes.md
          gh release create "$GITHUB_REF_NAME" \
            --title "$GITHUB_REF_NAME" \
            --notes-file /tmp/release-notes.md
